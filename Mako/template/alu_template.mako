/*
 * File: ${filename}
 * Author: ${author}
 * Description: ${description}
 */

#include <stdio.h>

// Function to ${module_name} two sources
int ${module_name} (${data_type} src1, ${data_type} src2) {
    
    ${data_type} res;

    res = src1 ${op} src2;

    return res;
}